# Copy to .env (docker compose reads it automatically) and fill in to enable Phase B AI dispatch.
# Optional: the stack runs without it — only the "Run AI dispatch" button (POST /api/dispatch/ai)
# calls the Anthropic API. Get a key at https://console.anthropic.com/.
ANTHROPIC_API_KEY=
